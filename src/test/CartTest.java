package test;

import java.util.ArrayList;
import java.util.Iterator;

import dao.BusDaoImpl;
import dao.CardDaoImpl;
import pojo.Bus;
import pojo.Customer;

public class CartTest {
	public static void main(String[] args) {
		/*CardDaoImpl c=new CardDaoImpl();
		boolean b=c.addCart(new BusDaoImpl().searchBusById(2), "sourav@mea");
		if(b)
			System.out.println("add");
		else
			System.out.println("fail");
		b=c.addCart(new BusDaoImpl().searchBusById(4), "sourav@mea");
		if(b)
			System.out.println("add");
		else
			System.out.println("fail");
		
		ArrayList<Bus> al=(ArrayList<Bus>) c.showCart("sourav@mea");
		Iterator<Bus> it=al.iterator();
		while (it.hasNext())
		{
			System.out.println(it.next());
		}
		 b=c.updateCartQuantity(2, 15);
		if(b)
			System.out.println("add");
		else
			System.out.println("fail");
		al=(ArrayList<Bus>) c.showCart("sourav@mea");
		it=al.iterator();
		while (it.hasNext())
		{
			System.out.println(it.next());
		}
		 b=c.clearCart("sourav@mea");
		if(b)
			System.out.println("add");
		else
			System.out.println("fail");
		al=(ArrayList<Bus>) c.showCart("sourav@mea");
		it=al.iterator();
		while (it.hasNext())
		{
			System.out.println(it.next());
		}*/
	}

}
